<!--components/song-sheet/song-sheet.wxml-->
<view class="container">  
  <text class="playCount">{{_count}}</text>
  <image src="{{song.picUrl}}" class="fengmian"></image>
  <view class="desciption">{{song.name}}</view>
</view>
